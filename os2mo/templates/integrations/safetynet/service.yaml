# SPDX-FileCopyrightText: Magenta ApS
#
# SPDX-License-Identifier: MPL-2.0
---
{{- if .Values.safetynet.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: safetynet-service
  labels:
    app: safetynet
  {{- with .Values.annotations }}
  annotations:
  {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  selector:
    app: safetynet
  ports:
    - name: "8000"
      port: 8000
      targetPort: 8000
{{- end }}
